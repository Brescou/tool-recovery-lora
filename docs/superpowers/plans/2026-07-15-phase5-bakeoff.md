# Phase 5 — Bake-off LoRA vs frontier prompting Implementation Plan

**Goal:** Same smoke set: LoRA vs Claude (prompting + tools API). Metrics: name/core/args exact, latency, estimated USD. GPT optional if `OPENAI_API_KEY` set.

**Deliverable:** `make bakeoff`, JSON results, notes + accuracy-vs-cost table for LinkedIn.
