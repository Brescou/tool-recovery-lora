# Phase 4 — LangGraph recovery demo Implementation Plan

**Goal:** Visible demo: bad tool call → tool error → LoRA repair → validated `meeting_prep` args.

**Note:** Do not replace `StructuredLLMPack` LLM with the LoRA (trained for tool calls, not `MeetingPrepOutput` JSON). Demo lives in this repo; stack remains the future host via an agent node that treats packs as tools.

**Deliverables:** `demo/` LangGraph, `scripts/run_demo.py`, `make demo`, notes.
